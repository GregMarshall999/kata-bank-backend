apiVersion: v1
kind: Service
metadata:
  name: kata-bank-api-service
  labels:
    app: kata-bank-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: kata-bank-api
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kata-bank-api-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - kata-bank-api.example.com
    secretName: kata-bank-api-tls
  rules:
  - host: kata-bank-api.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kata-bank-api-service
            port:
              number: 80
